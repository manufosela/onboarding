{
  /* Visit https://firebase.google.com/docs/database/security to learn more about security rules. */
  "rules": {
    ".read": "auth.provider === 'google' && auth.token.email.matches(/.*@leanmind.es$/)",
    ".write": "auth.provider === 'google' && auth.token.email.matches(/.*@leanmind.es$/)"
  }
}